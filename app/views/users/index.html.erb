<% provide(:title, 'All users') %>
<h1>All users</h1>
<%= render "search"%>

<%= render "excel" %>

<%= will_paginate %>

<ul class="users">
     <%= render @users %>     
</ul> 

<%= will_paginate %>
